import { CircleLoader } from "react-spinners";

function Loading() {
  return (
    <div>
      <CircleLoader className="my-20" color="#ffffff" size={150} />
    </div>
  );
}

export default Loading;
